{
  "name": "avalon_server",
  "version": "1.0.0",
  "description": "this is the ws server that handles storing all information",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alexander hicks",
  "license": "ISC",
  "dependencies": {
    "ws": "^3.2.0"
  }
}
